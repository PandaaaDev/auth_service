import { Injectable } from '@nestjs/common';

// import type { Module, ACL, Role } from './rbac_types';

@Injectable()
export class RbacService {
  // private getUserRoles(user_id: string) {
  //   //get user roles and return them in array
  // }
  // private isAdmin(user_id: string) {
  //   // check if user is admin
  // }
  // private getACLs(module: Module, user_id: string): ACL[] {
  //   //Query database for module ACL, check what roles user havedo and return it's access in form of an array
  //   return [];
  // }
  // private userRoles(user_id: string) {
  //   //query database and return user roles in array
  // }
  // accesibleModules(user_id: string) {
  //   // Query database and returns accesible models for the user with permisions
  // }
  // isValidAction(user_id: String, module: Module): boolean {
  //   const Acls = this.getACLs();
  //   //gather ACL, look for the module user is accessing and check if it's allowed action if yes return true if not return false
  //   return false;
  // }
  // validateAccess() {}
}
